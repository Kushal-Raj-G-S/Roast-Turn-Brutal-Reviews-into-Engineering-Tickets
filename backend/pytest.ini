[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: requires live external services (Kafka, GCS, BigQuery emulators)
